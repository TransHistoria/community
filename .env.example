# -----------------------------------------------------------------------
# Frontend environment variables
# Copy this file to .env.local for local development.
# In CI / GitHub Actions set these as repository variables (not secrets).
# -----------------------------------------------------------------------

# Full base URL of the Cloudflare Worker backend, no trailing slash.
# If left empty or unset the frontend falls back to:
#   https://communityapi.transhistoria.org
NEXT_PUBLIC_API_URL=https://transcommunity.cyanmint.workers.dev

# Optional secondary API base URL tried when the primary host is unreachable
# or blocked by client-side extensions / network policies.
NEXT_PUBLIC_API_FALLBACK_URL=

# Optional: base path when the site is served from a GitHub Pages sub-path.
# For a repo named "community" under org "TransHistoria" this would be
# "/community".  Leave blank if serving from a custom domain root.
NEXT_PUBLIC_BASE_PATH=

# Turnstile site key for signup/apply anti-bot verification.
# Preferred key name (CI secret): TURNSTILE_SITE_KEY
# Optional fallback name: NEXT_PUBLIC_TURNSTILE_SITE_KEY
TURNSTILE_SITE_KEY=
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
